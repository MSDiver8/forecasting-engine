# Forecasting Project

Проект для построения реального прогноза временного ряда с использованием тестирования моделей на различных горизонтах прогнозирования.

## 📂 Структура проекта

```
forecasting_project/
├── models/         # Модели прогнозирования (12 моделей)
│   └── models.py
├── utils/          # Вспомогательные функции: тестирование, отбор, объединение прогнозов
│   └── helpers.py
├── engine/         # Класс ForecastEngine для организации процесса прогнозирования
│   └── engine.py
├── main.py         # Скрипт для запуска всей системы
├── requirements.txt # Список используемых библиотек
└── README.md       # Описание проекта
```

## Описание проекта

Forecasting Project реализует процесс построения прогноза временного ряда следующим образом:

- На основе псевдовыборки проводится тестирование различных моделей прогнозирования на разных горизонтах.
- Оценивается качество каждой модели по каждому горизонту.
- Финальный прогноз строится как комбинация прогнозов лучших моделей, в зависимости от их результатов на разных горизонтах.
- Возможность получения отдельного прогноза по каждой модели.

Проект ориентирован на высокоточную работу с временными рядами и модульную архитектуру моделей.

##  Как запустить проект

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/MSDiver-8/forecasting_project.git
   cd forecasting_project
   ```

2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

3. Запустите основной скрипт:
   ```bash
   python main.py
   ```

## Используемые технологии

- Python 3.10+
- Pandas
- Scikit-learn
- Statsmodels
- Prophet
- Tensorflow / PyTorch
- LightGBM, XGBoost, CatBoost
- Matplotlib, Seaborn

##  Функционал

- Построение прогнозов с помощью 12 моделей
- Тестирование моделей на псевдовыборке для разных горизонтов
- Оценка качества моделей
- Динамическое объединение прогнозов на основе результатов тестирования
- Возможность отдельного вывода прогноза по каждой модели
- Расширяемая архитектура для добавления новых моделей

##  Планы по развитию

- Добавить кросс-валидацию моделей
- Автоматизация оптимизации гиперпараметров
- Построение веб-интерфейса через Streamlit
- Расширение набора моделей прогнозирования

---

Автор: [MSDiver_8](https://github.com/MSDiver-8)